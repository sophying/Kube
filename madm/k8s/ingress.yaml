apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: madm-ingress
spec:
  rules:
    - host: madm.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: madm-svc
                port:
                  number: 8080
